# 2026-05-07 薪資統計預設為全部設計

## 目標
- 薪資統計預設顯示全部資料。

## 範圍
- 檔案：`src/components/SalaryCalculator.tsx`
- 不變更：統計按鈕行為、統計計算與趨勢圖邏輯

## 設計
- `statsFilter` 初始值改為空字串 `''`（代表全部）。
- `statsRecords` 與既有統計計算保持不變。
- UI 預設高亮「全部」。

## 手動驗證
1. 進入薪資頁面，統計預設顯示全部資料。
2. 切換本月／上月，統計更新正常。
